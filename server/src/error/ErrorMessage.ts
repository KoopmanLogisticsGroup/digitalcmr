export enum ErrorMessage {
  defaultErrorMessage                 = 'Internal server error',
  loginErrorMessage                   = 'An error occurred during login',
  loginInvalidCredentialsErrorMessage = 'Invalid credentials',
  queryError                          = 'An error occurred while executing the query',
  invokeError                         = 'An error occurred while invoking the transaction'
}