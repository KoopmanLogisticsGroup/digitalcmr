---
apiVersion: v1
kind: Service
metadata:
  name: client-all
  labels:
    run: client-all
spec:
  type: NodePort
  selector:
    name: client-all
  ports:
  - protocol: TCP
    port: 4200
    nodePort: 31000
    name: http