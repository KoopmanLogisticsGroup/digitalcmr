---
apiVersion: v1
kind: PersistentVolume
metadata:
    name: ledger-pv
spec:
    capacity:
        storage: 100M
    accessModes:
        - ReadWriteOnce
    persistentVolumeReclaimPolicy: Recycle
    hostPath:
        path: "/var/hyperledger/ledger"