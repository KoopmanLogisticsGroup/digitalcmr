---
apiVersion: v1
kind: Service
metadata:
  name: api-doc
  labels:
    run: api-doc
spec:
  type: NodePort
  selector:
    name: api-doc
  ports:
  - protocol: TCP
    port: 8080
    nodePort: 31002
    name: http