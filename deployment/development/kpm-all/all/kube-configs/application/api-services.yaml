---
apiVersion: v1
kind: Service
metadata:
  name: api-all
  labels:
    run: api-all
spec:
  type: NodePort
  selector:
    name: api-all
  ports:
  - protocol: TCP
    port: 8080
    nodePort: 31002
    name: http