---
apiVersion: v1
kind: Service
metadata:
  name: privatedb-pon
  labels:
    app: privatedb-pon
    tier: db-pon
spec:
  type: NodePort
  selector:
    tier: db-pon
  ports:
  - protocol: TCP
    port: 5984
    name: http