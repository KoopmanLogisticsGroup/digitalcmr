apiVersion: v1
kind: PersistentVolume
metadata:
    name: couchdb-pv
    labels:
      vol: couchdb-pv
spec:
    capacity:
        storage: 1G
    accessModes:
        - ReadWriteOnce
    persistentVolumeReclaimPolicy: Recycle
    hostPath:
        path: "/var/couchdb-pv"