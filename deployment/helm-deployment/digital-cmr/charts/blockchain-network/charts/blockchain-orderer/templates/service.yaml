apiVersion: v1
kind: Service
metadata:
  name: {{ template "blockchain-orderer.fullname" . }}
  labels:
    app: {{ template "blockchain-orderer.name" . }}
    chart: {{ template "blockchain-orderer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "blockchain-orderer.name" . }}
    release: {{ .Release.Name }}
