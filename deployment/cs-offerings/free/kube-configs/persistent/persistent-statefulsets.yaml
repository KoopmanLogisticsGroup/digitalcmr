---
kind: StatefulSet
apiVersion: apps/v1beta1
metadata:
    name: persistent-couchdb-private
spec:
    serviceName: persistent-couchdb-private
    template:
        metadata:
            labels:
                tier: db
        spec:
            terminationGracePeriodSeconds: 10
            containers:
                - name: persistent-couchdb-private
                  image: couchdb:1.7.0
                  imagePullPolicy: Always
                  ports:
                    - containerPort: 5984
                      name: http
                  volumeMounts:
                    - name: couchdb-private-pvc
                      mountPath: /usr/local/var/lib/couchdb
    volumeClaimTemplates:
        - metadata:
            name: couchdb-private-pvc
          spec:
              accessModes:
                  - ReadWriteOnce
              resources:
                  requests:
                      storage: 10M
              storageClassName: ""