apiVersion: v1
kind: Service
metadata:
  name: {{ template "deployment-chart.fullname" . }}
  labels:
    app: {{ template "deployment-chart.name" . }}
    chart: {{ template "deployment-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "deployment-chart.name" . }}
    release: {{ .Release.Name }}
