---
# The service to expose composer playground
apiVersion: v1
kind: Service
metadata:
  name: composer-playground.kpm-pon
  labels:
    run: composer-playground.kpm-pon
spec:
  type: NodePort
  selector:
    name: composer-playground.kpm-pon
  ports:
  - protocol: TCP
    port: 8080
    nodePort: 31080
    name: http