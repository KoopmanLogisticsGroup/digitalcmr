---
apiVersion: v1
kind: Service
metadata:
  name: client-pon
  labels:
    run: client-pon
spec:
  type: NodePort
  selector:
    name: client-pon
  ports:
  - protocol: TCP
    port: 4200
    nodePort: 31000
    name: http